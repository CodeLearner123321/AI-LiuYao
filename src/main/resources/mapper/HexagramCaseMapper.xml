<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.divination.liuyao.mapper.HexagramCaseMapper">

    <!-- 按需查询指定文件下的卦例列表（仅返回展示所需字段） -->
    <select id="selectListBySourceFileId" resultType="com.divination.liuyao.pojo.vo.HexagramCaseListVO">
        SELECT id, case_index, start_offset, end_offset, case_text, ai_confidence, hexagram_number
        FROM hexagram_case
        WHERE source_file_id = #{sourceFileId} AND is_deleted = 0
        ORDER BY case_index ASC
    </select>

    <!-- 查询指定文件下的所有卦例（完整实体，供内部使用） -->
    <select id="findBySourceFileId" resultType="com.divination.liuyao.pojo.entity.HexagramCase">
        SELECT * FROM hexagram_case
        WHERE source_file_id = #{sourceFileId} AND is_deleted = 0
        ORDER BY case_index ASC
    </select>
</mapper>
