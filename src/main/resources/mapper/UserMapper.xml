<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.divination.liuyao.mapper.UserMapper">
    
    <!-- 结果映射 -->
    <resultMap id="userResultMap" type="com.divination.liuyao.pojo.entity.User">
        <id property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="passWord" column="pass_word"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="email" column="email"/>
        <result property="salt" column="salt"/>
        <result property="isVip" column="is_vip"/>
        <result property="balance" column="balance"/>
        <result property="frozenBalance" column="frozen_balance"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <!-- 根据手机号查询用户 -->
    <select id="findByPhoneNumber" resultMap="userResultMap">
        SELECT * FROM user WHERE phone_number = #{phoneNumber} LIMIT 1
    </select>
    
    <!-- 根据邮箱查询用户 -->
    <select id="findByEmail" resultMap="userResultMap">
        SELECT * FROM user WHERE email = #{email} LIMIT 1
    </select>
    
    <!-- 根据用户名查询用户 -->
    <select id="findByUserName" resultMap="userResultMap">
        SELECT * FROM user WHERE user_name = #{userName} LIMIT 1
    </select>
    
    <!-- 根据ID查询用户 -->
    <select id="findById" resultMap="userResultMap">
        SELECT * FROM user WHERE id = #{id} LIMIT 1
    </select>

    <!-- 插入新用户 -->
    <insert id="insert" parameterType="com.divination.liuyao.pojo.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (
            user_name, pass_word, email, salt, is_vip, balance, created_at, updated_at
        ) VALUES (
                     #{userName}, #{passWord}, #{email}, #{salt}, #{isVip}, #{balance}, #{createdAt}, #{updatedAt}
                 )
    </insert>
    
    <!-- 更新用户信息 -->
    <update id="update" parameterType="com.divination.liuyao.pojo.entity.User">
        UPDATE user
        SET user_name = #{userName},
            pass_word = #{passWord},
            phone_number = #{phoneNumber},
            email = #{email},
            salt = #{salt},
            is_vip = #{isVip},
            balance = #{balance},
            frozen_balance = #{frozenBalance},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    
    <!-- 删除用户 -->
    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
    
    <!-- 预扣费操作（使用乐观锁） -->
    <update id="preDeduct">
        UPDATE user
        SET balance = balance - #{amount},
            frozen_balance = frozen_balance + #{amount},
            updated_at = NOW()
        WHERE id = #{userId}
          AND balance >= #{amount}
    </update>
    
    <!-- 确认扣费操作 -->
    <update id="confirmDeduct">
        UPDATE user
        SET frozen_balance = 0,
            updated_at = NOW()
        WHERE id = #{userId}
          AND frozen_balance > 0
    </update>
    
    <!-- 退款操作 -->
    <update id="refund">
        UPDATE user
        SET balance = balance + #{amount},
            frozen_balance = frozen_balance - #{amount},
            updated_at = NOW()
        WHERE id = #{userId}
    </update>
    
    <!-- 充值操作 -->
    <update id="recharge">
        UPDATE user
        SET balance = balance + #{amount},
            updated_at = NOW()
        WHERE id = #{userId}
    </update>
    
</mapper> 